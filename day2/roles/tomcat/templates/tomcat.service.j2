[Unit]
Description=Tomcat
After=syslog.target network.target

[Service]
Type=forking

User=tomcat
Group=tomcat

ExecStart=/bin/bash {{ catalina_home }}/bin/startup.sh
ExecStop=/bin/bash {{ catalina_home }}/bin/shutdown.sh

[Install]
WantedBy=multi-user.target